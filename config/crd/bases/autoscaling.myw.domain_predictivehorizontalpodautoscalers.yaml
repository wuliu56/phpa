---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: predictivehorizontalpodautoscalers.autoscaling.myw.domain
spec:
  group: autoscaling.myw.domain
  names:
    kind: PredictiveHorizontalPodAutoscaler
    listKind: PredictiveHorizontalPodAutoscalerList
    plural: predictivehorizontalpodautoscalers
    shortNames:
    - phpa
    singular: predictivehorizontalpodautoscaler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PredictiveHorizontalPodAutoscaler is the Schema for the predictivehorizontalpodautoscalers
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PredictiveHorizontalPodAutoscalerSpec defines the desired
              state of PredictiveHorizontalPodAutoscaler
            properties:
              cpuInitializationPeriod:
                description: CpuInitializationPeriod defines the period seconds after
                  the pod starting when the pod is assumed being initialized, and
                  thus any transition into readiness is the first one. Default to
                  5 minutes.
                type: string
              delayOfInitialReadinessStatus:
                description: DelayOfInitialReadinessStatus defines the period seconds
                  after the pod starting when the pod is assumed to be still unready
                  after last transition into unreadiness. Defaults to 30 seconds.
                type: string
              horizontalScalePolicy:
                description: The policy of horizontal scaling.
                properties:
                  alpha:
                    description: Alpha for the DES algorithm. If not explicitly specified,
                      the auto adjustment will be applied to alpha according to collected
                      metrics.
                    type: string
                type: object
              maxReplicas:
                description: The upper limit for the number of replicas to which the
                  autoscaler can scale up.
                format: int32
                minimum: 1
                type: integer
              metrics:
                description: The target metric used to calculate the desired replica
                  count. Here multi-metrics haven't been applied, and only resource
                  metrics are supported.
                properties:
                  lowerTargetUtilization:
                    default: 0
                    description: The lower target utilization of the average of the
                      resource metric across all relevant pods, represented as a percentage
                      of the requested value of the resource for the pods. Defaults
                      to 0 and minimum is 0.
                    format: int32
                    minimum: 0
                    type: integer
                  name:
                    description: The name of the resource in question.
                    type: string
                  upperTargetUtilization:
                    default: 80
                    description: The upper target utilization of the average of the
                      resource metric across all relevant pods, represented as a percentage
                      of the requested value of the resource for the pods. Defaults
                      to 80 and minimum is 0.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - name
                type: object
              minReplicas:
                default: 1
                description: The lower limit for the number of replicas to which the
                  autoscaler can scale down. Defaults to 1 and minimum is 1.
                format: int32
                minimum: 1
                type: integer
              mode:
                description: 'Specifies the mode of scaling. Possible values are:
                  - "Horizontal" - "Vertical" - "Hybrid" Defaults to ''Hybrid'''
                enum:
                - Horizontal
                - Vertical
                - Hybrid
                type: string
              monitorWindowIntervalNum:
                default: 20
                description: The interval number of the monitor window used for resource
                  prediction. Defaults to 20 and minimum is 2.
                format: int32
                minimum: 2
                type: integer
              scaleDownStabilizationWindowSeconds:
                default: 120
                description: The minimal interval(second) between scaling down. Defaults
                  to 120 and minimum is 0.
                format: int32
                minimum: 0
                type: integer
              scaleHistoryLimit:
                description: The limit for the number of scaling history records.
                  Defaults to 20 and minimum is 0. This is a pointer to distinguish
                  between explicit zero and not specified.
                format: int32
                minimum: 0
                type: integer
              scaleTargetRef:
                description: Refers to the workload resource to be scaled.
                properties:
                  apiVersion:
                    description: API version of the referent
                    type: string
                  kind:
                    description: Kind of the referent
                    type: string
                  name:
                    description: Name of the referent
                    type: string
                required:
                - kind
                - name
                type: object
              tolerance:
                description: Only to scale when tolerance is smaller than the difference
                  between current utilization and target range. Represented as a percentage.
                  Defaults to 10 and minimum is 0. This is a pointer to distinguish
                  between explicit zero and not specified.
                format: int32
                minimum: 0
                type: integer
              verticalScalePolicy:
                description: The policy of vertical scaling.
                properties:
                  containerResourcePolicies:
                    description: Per-container resource policies.
                    items:
                      description: Specifies container resource policy.
                      properties:
                        containerName:
                          description: Name of the container or DefaultContainerResourcePolicy,
                            in which case the policy is used by the containers that
                            don't have their own policy specified.
                          type: string
                        maxAllowed:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: Specifies the maximal amount of resources that
                            the container can be scaled up to. The default is no maximum.
                          type: object
                        minAllowed:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: Specifies the minimal amount of resources that
                            the container can be scaled down to. The default is no
                            minimum.
                          type: object
                      type: object
                    type: array
                  evictionTolerance:
                    default: 50
                    description: Fraction of replica count that can be evicted for
                      update, if more than one pod can be evicted. Represented as
                      a percentage. Defaults to 50 and minimum is 1.
                    format: int32
                    minimum: 1
                    type: integer
                  serviceLabelSelector:
                    additionalProperties:
                      type: string
                    description: Label selector of the service that points to the
                      target pods.
                    type: object
                  type:
                    description: 'The policy type of vertical scaling. Possible values
                      are: - "Normal" - "Available" - "Safe" Defaults to ''Normal'''
                    enum:
                    - Normal
                    - Available
                    - Safe
                    type: string
                required:
                - type
                type: object
            required:
            - maxReplicas
            - metrics
            - mode
            - scaleTargetRef
            type: object
          status:
            description: PredictiveHorizontalPodAutoscalerStatus defines the observed
              state of PredictiveHorizontalPodAutoscaler
            properties:
              currentReplicas:
                description: The current number of replicas of the target workload.
                format: int32
                type: integer
              desiredReplicas:
                description: The desired number of replicas of the target workload.
                format: int32
                type: integer
              desiredResourceRequirements:
                additionalProperties:
                  description: ResourceRequirements describes the compute resource
                    requirements.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                      type: object
                  type: object
                description: The desired resource requirements for the target containers.
                type: object
              lastHorizontalScaleTime:
                description: The last time the PredictiveHorizontalPodAutoscaler horizontally
                  scales, used by the autoscaler to control the scaling frequency.
                format: date-time
                type: string
              lastMonitorTime:
                description: Refers to the time when the last metrics are fetched
                  and used to update PredictiveHorizontalPodAutoscalerStatus.
                format: date-time
                type: string
              lastVerticalScaleTime:
                description: The last time the PredictiveHorizontalPodAutoscaler vertically
                  scales, used by the autoscaler to control the scaling frequency
                  and ensure scaling completed.
                format: date-time
                type: string
              metricsList:
                description: Contains the collected metrics within the monitor window.
                items:
                  properties:
                    currentUtilization:
                      description: The current value of the resource metric across
                        all relevant pods, represented as a percentage of the requested
                        value of the resource for the pods.
                      format: int32
                      type: integer
                    currentValue:
                      anyOf:
                      - type: integer
                      - type: string
                      description: The current value of the resource metric across
                        all relevant pods, represented as the raw value sum of the
                        requested value of the resource for the pods.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    name:
                      description: The name of the resource in question.
                      type: string
                  required:
                  - currentUtilization
                  - name
                  type: object
                type: array
              scaleEventsList:
                description: Contains the recent scaling events.
                items:
                  description: ScaleEvent records the information of a scaling event.
                  properties:
                    replicas:
                      description: Count of desired replicas.
                      format: int32
                      type: integer
                    request:
                      anyOf:
                      - type: integer
                      - type: string
                      description: The desired pod request.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    time:
                      description: The time when a scaling happens.
                      format: date-time
                      type: string
                    type:
                      description: 'Type of scaling. Possible values are: - "Horizontal"
                        - "Vertical"'
                      type: string
                  required:
                  - replicas
                  - request
                  - time
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
